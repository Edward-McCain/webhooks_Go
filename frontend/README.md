# HookForge Dashboard

Nuxt 4 frontend for HookForge.

```bash
npm install
NUXT_PUBLIC_API_BASE=http://localhost:8080 \
NUXT_PUBLIC_DEFAULT_API_KEY=hf_hz97dupC37Crm281dt_6HZxOldO9baEnTMumbXl2nFM \
npm run dev
```

In Docker Compose the default API key is injected automatically.

See the root README for full stack setup.
