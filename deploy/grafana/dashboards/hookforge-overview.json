{
  "annotations": {
    "list": []
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 0 },
      "id": 1,
      "targets": [
        {
          "expr": "sum(rate(http_requests_total[1m]))",
          "legendFormat": "req/s",
          "refId": "A"
        }
      ],
      "title": "Requests / sec",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "sum(rate(webhooks_delivered_total[5m])) / clamp_min(sum(rate(webhooks_delivered_total[5m])) + sum(rate(webhooks_failed_total[5m])), 1)",
          "legendFormat": "success rate",
          "refId": "A"
        }
      ],
      "title": "Delivery success rate",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 0 },
      "id": 3,
      "targets": [
        {
          "expr": "histogram_quantile(0.50, sum(rate(http_request_duration_seconds_bucket[5m])) by (le))",
          "legendFormat": "p50",
          "refId": "A"
        },
        {
          "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket[5m])) by (le))",
          "legendFormat": "p95",
          "refId": "B"
        },
        {
          "expr": "histogram_quantile(0.99, sum(rate(http_request_duration_seconds_bucket[5m])) by (le))",
          "legendFormat": "p99",
          "refId": "C"
        }
      ],
      "title": "HTTP latency",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 8 },
      "id": 4,
      "targets": [
        { "expr": "sum(rate(webhook_retries_total[5m]))", "legendFormat": "retries/s", "refId": "A" }
      ],
      "title": "Retry rate",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 8 },
      "id": 5,
      "targets": [
        { "expr": "active_workers", "legendFormat": "active", "refId": "A" },
        { "expr": "queue_depth", "legendFormat": "queue", "refId": "B" }
      ],
      "title": "Worker utilization",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 8 },
      "id": 6,
      "targets": [
        { "expr": "sum(rate(kafka_messages_processed_total[5m]))", "legendFormat": "processed", "refId": "A" },
        { "expr": "sum(rate(kafka_messages_failed_total[5m]))", "legendFormat": "failed", "refId": "B" }
      ],
      "title": "Kafka processing",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["hookforge"],
  "timezone": "browser",
  "title": "HookForge Overview",
  "uid": "hookforge-overview",
  "version": 1
}
